name: nyc_taxi
version: '1.0.0'
config-version: 2

profile: nyc_taxi

model-paths: ["models"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  nyc_taxi:
    bronze:
      +materialized: view
    silver:
      +materialized: table
    gold:
      +materialized: table